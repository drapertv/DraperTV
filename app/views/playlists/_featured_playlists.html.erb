<div class="featured-playlists">
	<% VideoFeature.currently_featured_playlists.each do |video_feature| %>
	  <% video = video_feature.video %>
	  <div class="featured-playlist">
		  <div class="featured-playlist-title">
		  	<%= video.playlist.title %>
		  </div>
		  <div class="featured-playlist-main">
			  <div class="series-speaker">
			    <%= image_tag video_feature.profilepic_url(:seriesthumb), :class => "pull-left speaker-pic" %>
			    <div class="speaker-name">
			     <%= video_feature.name %>
			    </div>
			    <div class="speaker-title">CEO & Founder Zappos</div>
			  </div>
			  <div class="video-item-container">
				  <div class="video-img">
				    <% if video.can_be_accessed_by? current_user %>
				       <a href="<%= video_path(video) %>">
				          <%= image_tag 'play-icon.png', :class => "video-play" %>
				       </a>
				    <% else %>
				      <%= image_tag 'play-icon.png', :class => "video-play blocked" %>
				      <div class="modal modal-overlay">
				        <div class="modal-content video-modal">
				          <div class="modal-message">This video is video is only available for premium users. Click here to get premium access for $9.99.</div>
				        <%= link_to video_increment_demand_path(video), remote: true do %>
				          <div class="btn-primary btn m-t-lg">
				            Get Access
				          </div>
				        <% end %>
				        </div>
				      </div>
				    <% end %>
				    <span class="section-time"> 3:45 </span>
				    <a href="#"><img src="<%= video.vthumbnail %>"  alt="" class="video-thumbnail"></a>
				  </div>
				  <div class="video-info">
				    <a href="#" class="text-ellipsis qwatch-title"><%= video.title %></a>
				    <p class="text-ellipsis video-stats"> 3045 Views * <%= video.get_upvotes.size %> <i class="fa fa-star stared"></i></p>
				  </div>
				</div>
			</div>
		</div>
	<% end %>
</div>