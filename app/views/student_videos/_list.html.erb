<% @student_videos.each do |content| %>
  <%= link_to media_path(content), :class => "disabled" do %>
    <div class="media-thumbnail <%= content.session_name %>" data-date="<%= content.seconds_elapsed_since_creation %>" data-speaker="<%= content.speaker_name %>" data-views="<%= rand(10000) %>">
      <div class="media-info">
        <div class="media-speaker-name">
          <%= content.student_name %>
        </div>
        <div class="media-title">
          <%= content.title %>
        </div>
      </div>
      <%= image_tag content.thumbnail, :class => "thumbnail-img" %>
      <div class="thumbnail-gradient"></div>
      <div class="thumbnail-overlay-gradient"></div>
      <div class="thumbnail-description"><p><%= content.html_description %></p></div>
    </div>
  <% end %>
<% end %>


